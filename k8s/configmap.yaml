apiVersion: v1
kind: ConfigMap
metadata:
  name: theme-config
data:
  theme.css: |
    :root {
      --primary-color: #3b82f6;  /* BLUE! */
      --bg-card: rgba(59, 130, 246, 0.15);
      --border-card: rgba(96, 165, 250, 0.4);
    }
